# Context Reset Rule

Agents must rely on project files, not chat memory.

Before work, read `PROJECT_FILE_INDEX.md`, `NEXT_ASSIGNMENT.md`, relevant assignment, relevant existing docs, and latest reports.

Write enough status back to disk so another agent can continue later without prior chat context.
